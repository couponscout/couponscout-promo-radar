# Local virtualenv used only for running the scripts on a dev machine.
# The pipeline itself is pure stdlib and needs nothing installed.
.venv/
__pycache__/
*.pyc

# Build output is regenerated by build.py and committed deliberately, so it is
# NOT ignored - it is what Cloudflare Pages serves.
